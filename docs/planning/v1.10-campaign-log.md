# v1.10 Campaign Log

Running record of the first full-library enrichment campaign, per
`docs/planning/v1.10-charter.md` (Phase 2 mechanics). One entry per wave:
date, step, batch size, applied/declined/skipped, cost actual vs.
projected, calibration notes. This file is the v1.11 re-audit's primary
input.

## Campaign model decision (charter item 2, decided 2026-06-10)

**Sonnet 4.6 (`claude-sonnet-4-6`) runs the campaign** — the model
`config.json` already pins, so no config change. Decided by James from
the PR #74 cost table (full-sweep projection: Opus 4.8 ≈ $209, Sonnet
4.6 ≈ $97, Haiku 4.5 ≈ $32).

Reasoning for the record:

- All calibration data, prompt tuning, and the rate-limit history were
  earned on Sonnet.
- The pipeline structurally bounds model quality everywhere except
  comments prose (catalog-sourced LCC, description grounding,
  truncation, review gates).
- Regrade + the audit log make a future model upgrade a re-run, not a
  do-over.

**Carve-out — comments-enrich model revisited at that wave's boundary.**
Before the first full comments wave: run a ~50-book Opus 4.8 vs Sonnet
4.6 comparison on real books (dry-run, ~$1); James picks the
comments-step model on the evidence. Steps 1–3 (clean-titles,
enrich-identifiers, lcc-enrich) are committed to Sonnet now.

Chosen-model smoke (2026-06-10): `clean-titles --limit 5 --dry-run` on
`claude-sonnet-4-6` rendered the dollar estimate (≈ $0.04) against the
refreshed price table.

## Waves

*(none yet — campaign starts after the v1.10.0 release and usage-tier
confirmation)*
